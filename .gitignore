node_modules/
.superpowers/
**/fixtures/*.tmp
.DS_Store

# never let pi state or credentials in, even if someone runs this from ~/.pi/agent
auth.json
models-store.json
sessions/
trust.json

# API keys for extensions — this repo is public
.env
**/.env

# scratch working state (local ticket tracker, etc.)
.scratch/
