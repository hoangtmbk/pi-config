## Task 1: Test infrastructure + fidelity fixtures (RED)

Goal: a fixture-based `node:test` suite that asserts **content invariants** on real pages, so every later fix is locked in. Most tests are expected to FAIL at the end of this task — that is correct; verify each fails for the documented defect, not for a harness bug.

Files:
- `package.json`: add `"scripts": {"test": "tsx --test tests/*.test.ts", "typecheck": "tsc --noEmit"}`, devDependencies `tsx`, `typescript`, `@types/turndown`, `@types/node`. Run `npm install`.
- `tsconfig.json`: `"module": "NodeNext"`, `"moduleResolution": "NodeNext"`, `"allowImportingTsExtensions": true`, `"noEmit": true`, `"strict": true`, `"target": "ES2022"`, `"lib": ["ES2022", "DOM"]`, `"types": ["node"]`, and `paths` mapping `@earendil-works/pi-coding-agent` → the installed pi package `dist/index.d.ts` (find via `npm root -g`; write the absolute path — this tsconfig is local-only), `@earendil-works/pi-tui` → pi's bundled `node_modules/@earendil-works/pi-tui`, `typebox` → pi's bundled typebox. Fix the one existing tsc error (`extract.ts:47` `"svg"` is not a valid Turndown tag filter — replace the `dropNonContent` filter with a function `(node) => ["SVG","CANVAS",...].includes(node.nodeName)` or drop `svg` since it is already removed at DOM level). `npx tsc --noEmit` must pass on the baseline code (with that one fix).
- `tests/helpers.ts`: `loadFixture(name): FetchedPage` building `{url, finalUrl, status, contentType: "text/html", charset: "utf-8", body, bytes}` (check `FetchedPage` in fetch.ts for exact fields) from `tests/fixtures/<name>.html`; `fences(md)` returning `{lang, code}[]` parsed from ```` ``` ```` blocks; `headings(md)` returning `{level, text}[]`; `hasEscapedMarkdownInFences(md)` (true if any fence body contains `\[`, `\]`, `\>`, `\*`, `\_` produced by escaping).
- `tests/fixtures/`: copy from `../web-fetch-review/qa-out/`: `03-docs.python.org.raw.txt` → `python-asyncio.html`; `05-github.com.raw.txt` → `github-readability.html`; `19-react.dev.raw.txt` → `react-learn.html`; `01-en.wikipedia.org.raw.txt` → `wikipedia-transformer.html`; `21-go.dev.raw.txt` → `go-tutorial.html`; `10-pypi.org.raw.txt` → `pypi-requests.html`; `14-news.ycombinator.com.raw.txt` → `hn-front.html`; `02-developer.mozilla.org.raw.txt` → `mdn-fetch.html`; `40-docs.anthropic.com.raw.txt` → `claude-docs-prompt-caching.html` (this is the platform.claude.com page where Readability returns only the sidebar; confirm by looking at `40-docs.anthropic.com.md`); `51-kakaku.md.raw.txt` → keep as bytes `kakaku-sjis.html` only if it is the raw undecoded bytes (check; if it is already-mojibake text, instead synthesize `meta-sjis.html` by writing Shift_JIS bytes with `iconv`-free approach: `Buffer.from(new TextEncoder()...)` is UTF-8 only, so instead create the fixture with Node: `require('iconv-lite')` is NOT allowed — use a hand-built byte array for the string "日本語テスト" in Shift_JIS: `93 FA 96 7B 8C EA 83 65 83 58 83 67`). Synthetic fixtures written by hand: `code-regex.html` (a `<pre><code class="language-cpp">` block containing exactly these lines: `auto f = [](int a) { return a; };`, `arr[i]();`, a blank line, then `// -----` , then `int x = 1;`, and a second bare `<pre>` without `<code>` containing `let v = vec![1, 2];` and `>>> print("hi")`), `table-td-only.html` (a `<table>` with `<caption>Versions</caption>` and 3 rows × 3 `<td>` cells, no `<th>`, plus a nested single-row layout table wrapping a paragraph), `fragment.html` (HTML with no `<html>`/`<body>`: just `<h1>Title</h1><p>one</p><p>two</p><pre>code</pre>`), `div-code.html` (a `<pre>` whose lines are `<div>` children, and another `<pre>` using `<br>` between lines, and a `<pre><code>` where each line is a `<span class="line">`).
- `tests/fidelity.test.ts` — one `describe` per fixture; assertions (use exact numbers only where you have measured them from the fixture; otherwise use `>=` bounds you verified by inspecting the HTML):
  - code-regex: markdown contains verbatim `auto f = [](int a) { return a; };`, `arr[i]();`, `// -----`, `let v = vec![1, 2];`, `>>> print("hi")`; the blank line inside the first fence is preserved; first fence has lang `cpp`; `hasEscapedMarkdownInFences` is false; exactly 2 fences.
  - python-asyncio: ≥ 1 fence; contains verbatim a code line you pick from the page's HTML (e.g. an `import asyncio` line inside a `<pre>`); no escaped markdown in fences; ≥ 1 fence with lang `python` (Sphinx uses `highlight-python`/`highlight-default` wrappers — measure what the fixture has; if no language class exists anywhere for a block, do not assert lang on it).
  - github-readability: README heading present; ≥ 1 fence; a code line from the README verbatim; no escaped markdown in fences.
  - react-learn: pick a multi-line code block from the HTML; assert those lines appear on separate lines inside one fence (not collapsed).
  - div-code: each `<pre>` yields a fence with lines on separate lines; `span.line` case yields separate lines.
  - go-tutorial: ≥ 1 fence; a verbatim `go mod init` (or similar) command line; no escaped markdown in fences.
  - pypi-requests: fence containing `>>> ` line unescaped.
  - wikipedia-transformer: h2 headings present for ≥ 8 of the article's sections (list them from the HTML `<h2>` ids, e.g. `History`, `Training`, `Architecture`…); markdown does not contain `[edit]`; no heading ends with `#`, `¶`, or U+200B.
  - claude-docs-prompt-caching: markdown length ≥ 20,000 chars; contains an article phrase (pick one from the HTML body, e.g. `cache_control`); mode/extraction did not return the sidebar only.
  - table-td-only: markdown contains a GFM table with 3 data rows (count lines starting with `|`), cell values on the same row; `Versions` caption text present; the nested layout table's paragraph is present as plain text (no `|`).
  - fragment: markdown contains `# Title`, `one`, `two`, and a fence with `code`.
  - hn-front: markdown does not contain a `|`-table (layout tables unwrapped) and contains ≥ 25 links `](https://` (front page has 30 stories) — measure.
  - mdn-fetch: ≥ 3 fences; a verbatim `fetch(` code line.
- `tests/charset.test.ts`: imports `decodeBody` from `../fetch.ts` (exported `decodeBody(buffer: Uint8Array, headerCharset: string | undefined): string`; this export does not exist yet — the test will fail on import, which is expected): asserts Shift_JIS bytes with no header charset but `<meta charset="shift_jis">` decode to `日本語テスト`; `<meta http-equiv="Content-Type" content="text/html; charset=gb2312">` variant with bytes `D6 D0 CE C4` → `中文`; header charset wins over meta; default utf-8.
- Report which tests pass/fail and, per failing test, the one-line reason (must match a defect in `ACTION-PLAN.md` Phase 1). If a test fails for a harness reason, fix the harness.

Commit: `test: fixture-based fidelity suite (red) + tsconfig`.

